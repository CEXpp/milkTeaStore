# 2026-09-13 工作记录

## T04 后端统一响应 / 全局异常 / 健康检查（完成代码，未编译验证）
- **需求复述**：为 `com.milktea.order` 后端补齐统一响应体 + 全局异常（业务/校验/未知）+ actuator 健康检查，响应体与错误码须对齐 LLD 3.1 / 3.2；保留现有 `R.java`；Java 升 25；审计依赖对 Boot 4.1.1 适配。
- **关键发现**：用户未拉取的 `origin/main` 已重构为 `com.milktea.order` + MyBatis-Plus，且已有 `R.java`（即 LLD 3.1 响应体）。LLD 文档不在仓库，位于项目资产网盘《详细设计说明书》(file_id `CQrBJdzzhpML`)，已下载并提取 3.1/3.2 章节。
- **改动**：
  - `pom.xml`：`java.version` 17→25；移除 `spring-boot-starter-data-jpa`；新增 `actuator`、`validation`、`jdbc`（JPA 移除后补 jdbc 防数据源缺失）。
  - 新增 `common/exception/ErrorCode.java`（LLD 3.2 错误码枚举）、`BusinessException.java`、`GlobalExceptionHandler.java`（@RestControllerAdvice，三类异常→R.fail，HTTP 200）。
  - 新增 `common/config/WebConfig.java`（CORS，来源读 `app.cors.allowed-origins`）。
  - `application-dev.yml`：移除 `spring.jpa.*`，新增 `management.endpoints.web.exposure.include=health` 与 `app.cors.allowed-origins=*`。
- **自检**：实体类均用 MyBatis-Plus 注解（@TableName/@TableId），无 jakarta.persistence 引用，移除 JPA 安全；`R.fail(int,String)` 签名与处理器调用一致；补 validation 使 `jakarta.validation.ConstraintViolationException` 可编译并触发。
- **未验证**：本环境无 Maven / 仅 JDK 21，未编译运行。需用户侧（JDK 25 + Maven）`mvn clean package` 后 `curl http://<IP>:8080/actuator/health` 验证。
- **偏离说明**：工单仅需「补 actuator」，但为让「校验异常 1001」处理器可编译并真正生效，额外补了 `spring-boot-starter-validation`（必要修正）。

## 文档治理
- 新建 `.workbuddy/instructions.md`（导航：设计文档在项目资产网盘，附 file_id；指向 MEMORY.md）。
- 新建 `.workbuddy/memory/MEMORY.md`（项目技术栈/约定）。

## T04 提交与共享计划更新
- 提交：`42d99c8`（本地 `main`，未 push）。范围：pom / application-dev.yml / WebConfig / exception/* / .workbuddy / .gitignore(追加 .idea/)。
- 刻意排除 `docker/.env.example`：该文件把占位符替换为真实凭据(MYSQL_ROOT_PASSWORD=1234、MINIO_ROOT_PASSWORD=12345678)，提交有泄露风险，已留工作区待 owner 决定。
- 共享计划面板 T04(`rOgOrI`)：状态 `running`→`完成(done)`；追加评论记录改动与凭据排除说明(comment_id cmt_2010dafd...)。
- 发送完成动态(msg_ad28749a...)：交付内容 + 凭据排除安全提示 + 验证待办。

## 共享计划标签 + 版本升级通知
- 为 T01–T38 全部 38 个任务批量打标签（4 正交维度：端/域/阶段/风险），维度与固定取值已固化进 MEMORY.md。
  - 端：`后端-Java`/`前端-小程序`/`前端-商家Web`/`基础设施`
  - 域：`账号域`/`商品域`/`订单域`/`支付域`/`AI域`/`统计域`
  - 阶段：`基建`/`开发`/`联调`/`验收`/`发布`
  - 风险：`阻塞`/`需评审`/`依赖外部`/`待验证`（仅 T28 联调、T37 验收打 `待验证`；T36 发布打 `依赖外部`）
- 用户指出「任务评论无提示、需点开详情才看得到」，明确通知分工：**动态=主通知(广播+提示)，面板标签=长期分类，任务评论=仅细节补充**。
- 发版本升级主通知动态(msg_59512252...)：工单 `com.example.backend` 旧脚手架 与 实际 `com.milktea.order`+Boot 4.1.1+MyBatis-Plus 不一致；已升级 Java 17→25、移除 data-jpa、补 actuator/validation/jdbc；提示工单 S 列应回填实际基线。

## 小程序初始化（uni-app）[T-miniprogram]
- **需求复述**：用户在根目录误建了非 uni-app 的微信原生小程序配置（project.config.json / project.private.config.json），需删除后用 uni-app 在 backend 同级重建。
- **删除**：根目录 `project.config.json`、`project.private.config.json`（微信原生，`compileType:miniprogram`，未 git 跟踪，安全移除）。
- **重建**：在 `D:\Project\milkTeaStore\miniprogram`（与 backend 同级）用官方 `uni-preset-vue#vite-ts` 脚手架（Vue3+TS）；`manifest.json` 已写入用户原 appid `wxcd3beb616feadd06`、name「奶茶在线点单系统」。`dev:mp-weixin`/`build:mp-weixin` 脚本就绪。技术栈已补进 MEMORY.md。
- **环境结论（重要）**：本沙箱的 `npm install` 不落盘到用户机器，`miniprogram/node_modules` 需**用户本机 PowerShell** 自行 `npm install` 后 `uni` 命令才可用（用户已实测 `uni 不是内部或外部命令` 印证）。调试方式：编译 `dist/dev/mp-weixin` → 微信开发者工具导入，非浏览器网址。

## T06 JWT 过滤器链（双体系路径矩阵）[后端-Java / 账号域 / 开发]
- **需求复述**：实现 JWT 过滤器链，按 LLD 9.1 / HLD 5.1「双体系路径矩阵」分派——`/api/admin/**` 需 role=admin，`/api/customer/**` 需顾客 token，白名单（login/menu/shop-status/files）放行；并补 admin 登录（账密+BCrypt+失败5次锁10分钟）；401/403 走统一响应体。
- **权威依据**（已下载 HLD/SRS/排期核对）：HLD 5.1 分组与鉴权体系、HLD 9.1 安全（双 JWT HS256 + 路径矩阵过滤器、BCrypt、失败锁）、排期 T06 验收点（顾客 token 访问 /api/admin/** 返 403；无 token 访问订单返 401；admin 登录错 5 次锁定）；LLD（file_id `CQrBJdzzhpML`）下载链接服务端签名持续失败，未取到 9.1 原文，以上事实已交叉印证一致。
- **依赖缺口处理**：T06 所需的 `JwtUtil` 原属 T05 产物，库中不存在，故在此一并落地（共用），T05 顾客登录端点仍为其独立任务。
- **改动**（提交 `ae6c533`，本地 main，未 push）：
  - `common/jwt/`：JwtUtil（HS256 双体系签发/解析，密钥 `${jwt.secret}`≥32字节）、JwtAuthenticationFilter（OncePerRequestFilter，按矩阵分派，401/403 真实 HTTP 状态码 + 统一 R 体）、AuthContext（ThreadLocal 身份）。
  - `user/`：AdminUser 实体/Mapper、AdminAuthService（BCrypt + 内存失败计数锁10分）、AdminAuthController（POST /api/admin/login）、AdminLoginRequest/AdminLoginVo。
  - GlobalExceptionHandler：业务异常 code=401/403 改以真实 HTTP 401/403 承载（落实 HLD 5.2）。
  - `pom.xml`：jjwt 0.12.6（api/impl/jackson）+ spring-security-crypto（仅 crypto，不引入 Web Security 链）。
  - `application-dev.yml`：注入 jwt.secret、admin.initial-username=admin、admin.initial-password-hash（admin123 的 BCrypt 哈希，供 Flyway V2 种子）。
- **凭证文件处理**：`docker/.env.example`（含真实凭据 1234/root/12345678）先前被 agent 留工作区未提交；本次依用户指示已补 `JWT_SECRET` 并随提交 `78fa76a` 提交（用户明确"之前都让我改"）。安全提示：该文件含真实凭据，若仓库对外共享建议改占位符或 `.gitignore` 排除 `.env`。未提交 memory/docs/tmp/miniprogram 预存改动。
- **未验证**：本环境无 Maven / JDK 25，未编译。dev 验证：`curl -X POST http://<IP>:8080/api/admin/login -H 'Content-Type: application/json' -d '{"username":"admin","password":"admin123"}'` 应返 code=0 + token；顾客 token 访问 /api/admin/** 应返 403；无 token 访问订单接口应返 401。
- **待办/依赖**：① T05 顾客登录端点（/api/customer/login、dev-login）签发顾客 token 后，过滤器顾客分支方可端到端验证；② 共享计划面板 T06(`rCKvRo`) 已用 `mcp__wb-issues__todo_transition` 置为「完成(done)」，并完成动态周知(msg_ec957db67df0474ca8dff5355a72db32)。记：项目待办/动态工具确实存在（todo_*/project_message_*），勿再误判"无工具"。

## 本地变动按任务划分提交并推送
- 未提交变动三处：① `miniprogram/src/pages/index/index.vue`（`Hello`→`Hello2` 首页占位文案微调）→ 归入 **T24 小程序工程与请求层**；② `.workbuddy/docs/`（HLD.docx 1.5MB + SRS.docx + ref 提取稿，网盘缓存）→ 用户确认**排除**，`.gitignore` 追加 `.workbuddy/docs/`；③ 本工作记录。
- 提交拆分 2 笔：提交 A `[T24]` 首页文案微调；提交 B `chore` 排除 docs 缓存 + 更新工作记录。
- 推送：`git push origin main`（本地 main 领先 origin/main 共 22 提交，含 T04/T06 等已落地未推送内容；普通推进，非强推）。
- ⚠️ 安全提示：待推送历史含 `docker/.env.example` 真实凭据（MYSQL_ROOT_PASSWORD=1234、MINIO_ROOT_PASSWORD=12345678），会随 push 公开至 GitHub。建议：① 后续 `.gitignore` 排除 `.env*` 模板或改占位符；② 立即轮换弱口令；③ 若仓库对外可见，用 `git filter-repo` 清理历史中该文件。
